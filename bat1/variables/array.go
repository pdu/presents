package main

import "fmt"

func main() {
	var arr []byte = make([]byte, 10)
	var nums []int = []int{1, 2, 3}

	var mapping map[string]int = make(map[string]int)
	mapping["apple"] = 3
	mapping["pear"] = 5

	fmt.Println("vim-go")
}
